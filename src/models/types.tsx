export interface Flight {
  number: string;
  duration: string;
  airlines: string;
  departureDate: string;
  arrivalDate: string;
  departure: string;
  arrival: string;
  departureTime: string;
  arrivalTime: string;
}
